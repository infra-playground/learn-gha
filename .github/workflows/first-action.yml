name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "hello gh actions"
    - name: Print dispedido
      run: echo "Done - See ya!"
  
  
